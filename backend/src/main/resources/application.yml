server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fluffyletter?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: fluffy
    password: fluffy123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

fluffy:
  wechat:
    appid: "wx4b588490e6e63317"
    secret: "ead72b3a3e2d2d6898bbee786cc437ee"
  jwt:
    # 至少 32 字符，生产环境请用强随机值
    secret: "0e198b5279713dfa549e6d39313ae0b1b30b24885db880de8a2b1c48f550b718"
    ttlSeconds: 2592000 # 30 天
  admin:
    bootstrap:
      # 可选：开发期启动时，如果 admin_user 表为空，则创建初始管理员。
      # 建议只在本地使用，并通过环境变量注入。
      username: ${ADMIN_BOOTSTRAP_USERNAME:admin}
      password: ${ADMIN_BOOTSTRAP_PASSWORD:admin123}
      role: ${ADMIN_BOOTSTRAP_ROLE:superadmin}
